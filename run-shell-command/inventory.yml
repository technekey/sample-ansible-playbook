all:
  children:
    localhosts:
      hosts:
        localhost:
          ansible_host: localhost
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    ungrouped: {}
